#include <limits.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

#include "defines.h"

typedef struct {
    u64* digits;
    u64 len;
} BigNum;

int main(void) {
    // printf("%lu\n%llu\n", ULONG_MAX, ULLONG_MAX);
    return 0;
}
